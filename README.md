# API de Gerenciamento de Tarefas em Spring Boot

Endpoints:

- `POST /tasks` 
- `GET /tasks`
- `GET /tasks/{id}`
- `PUT /tasks`
- `DELETE /tasks`


Representação em JSON da entidade `Task`:

```json
{
  "id": 1,
  "description": "Minha tarefa supimpa",
  "checked": true
}
```
